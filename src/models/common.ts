// src/models/common.ts  
// Роли
export interface Role {
  id: number;
  name: string;
}
// Должности
export interface Position {
  id: number;
  name: string;
}
// Подразделения
export interface Subdivision {
  id: number;
  name: string;
}